# Feature Dependency Graph

Paper trading
→ needs portfolio engine
→ needs order simulation
→ needs market data
→ needs historical candles
→ needs storage and replay

AI scoring
→ needs feature store
→ needs market data
→ needs labels
→ needs backtesting
→ needs validation
